#problems concurrency
Problems:
- Race condition (Состояние гонки)
- Resource contention (конкуреция за ресурс)
- Priority inversion (Инверсия приоритетов)
- Deadlock (Взаимная блокировка)
- Livelock (Активная блокировка)
- Starvation (Голодание)
- Data Race (Гонка за данными)

Problems:
  - Race condition (Состояние гонки)
      несколько потоков работают с одними данными, данные становятся непредсказуемыми зависят от порядка выполнения потоков.

  - Resource contention (конкуреция за ресурс)
      Несколько потоков, выполняющих разные задачи, пытаются получить доступ к одному ресурсу, тем самым увеличивая время необходимое для безопасного получения ресурса. Эта задержка может привести к непредвиденному поведению.

  - Priority inversion (Инверсия приоритетов)
      Поток с низким приоритетом удерживает ресурс, которые требуется другому потоку с более высоким приоритетом.

  - Deadlock (Взаимная блокировка)
      Несколько потоков блокируют друг друга.

  - Livelock (Активная блокировка)
      Возникает тогда, когда программа выполняет параллельно несколько операций, но эти операции не продвигают программу к завершению. То есть потоки просто тратят свое время друг на друга, совершая, по факту, бесполезную работу.

  - Starvation (Голодание)
      Поток не может получить доступ к ресурсу и безуспешно пытается сделать это снова и снова.

  - Data Race (Гонка за данными)
      состояние, когда один поток обращается к изменяемому объекту, в то время как другой поток записывает в него.

Источник: https://ios-interview.ru/multithreading-problems/
https://medium.com › всё-о-многопоточности-в-swif...
