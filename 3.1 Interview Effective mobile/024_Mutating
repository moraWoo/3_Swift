#mutating
mutating - это ключевое слово в Swift, которое используется для изменения свойств структуры или перечисления (enum) внутри метода. Когда мы создаем метод для структуры или перечисления, Swift по умолчанию не позволяет изменять свойства экземпляра изнутри метода. Чтобы разрешить изменение свойств, необходимо использовать ключевое слово mutating при объявлении метода.

В Swift структуры и перечисления являются значениями (value types), и поэтому копируются при передаче в функции. Это означает, что методы структуры и перечисления не могут изменять свойства экземпляра, потому что это противоречит концепции значений. Если мы хотим изменять свойства экземпляра внутри метода, мы должны использовать ключевое слово mutating.

Например, мы можем создать структуру Point с двумя свойствами x и y, а затем создать метод moveBy(x:y:), который изменяет значение свойств x и y:

struct Point {
    var x = 0.0, y = 0.0

    mutating func moveBy(x deltaX: Double, y deltaY: Double) {
        x += deltaX
        y += deltaY
    }
}

Здесь мы используем ключевое слово mutating при объявлении метода moveBy(x:y:). Это означает, что метод изменяет свойства экземпляра структуры, что было бы невозможно без использования ключевого слова mutating. Метод moveBy(x:y:) изменяет свойство x на значение deltaX и свойство y на значение deltaY.

Когда мы используем метод moveBy(x:y:), мы можем изменить свойства структуры, используя следующий код:
var point = Point(x: 10.0, y: 20.0)
point.moveBy(x: 5.0, y: 10.0)
print("The point is now at (\(point.x), \(point.y))") // "The point is now at (15.0, 30.0)"

Здесь мы создаем экземпляр структуры Point, изменяем его координаты с помощью метода moveBy(x:y:), и затем выводим новые значения координат на консоль.

Важно отметить, что ключевое слово mutating можно использовать только в методах структур и перечислений. Методы классов, которые изменяют свойства экземпляра, не требуют ключевого слова mutating, потому что классы являются ссылочными типами (reference types), и экземпляры классов передаются по ссылке, а не по значению, поэтому методы классов могут изменять свойства экземпляра без использования ключевого слова mutating. Кроме того, стоит отметить, что структуры и перечисления могут содержать только свойства и методы экземпляра, в то время как классы могут содержать также статические (static) свойства и методы класса.
